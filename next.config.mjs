/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: {
        domains: ['www.istockphoto.com' , 'example.com' ,'media.istockphoto.com']
    }
};

export default nextConfig;
